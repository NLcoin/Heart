<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="@dimen/chat_emoji_height">

    <android.support.v4.view.ViewPager
        android:id="@id/face_viewPager"
        android:layout_width="fill_parent"
        android:layout_height="@dimen/chat_emoji_pager_height"
        android:layout_gravity="center_horizontal"
        android:paddingBottom="@dimen/chat_emoji_pager_padding"
        android:paddingLeft="@dimen/chat_emoji_pager_padding"
        android:paddingRight="@dimen/chat_emoji_pager_padding"
        android:paddingTop="@dimen/chat_emoji_pager_padding" />

    <com.meelive.ingkee.ui.widget.emoji.EmojiIndicatorView
        android:id="@id/face_indicator"
        android:layout_width="fill_parent"
        android:layout_height="@dimen/chat_emoji_indicator_height"
        android:layout_marginTop="@dimen/chat_emoji_pager_height"
        android:gravity="center" />
</FrameLayout>